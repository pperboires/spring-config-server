server:
  port: 8888

management:
  context-path: /server
  
logging:
  level:
    com.netflix.discovery: 'OFF'
    org.springframework.cloud: 'DEBUG'


spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pperboires/spring-config-server/config-repo/{application}
          basedir: target/config

---
spring:
  profiles: cloud
